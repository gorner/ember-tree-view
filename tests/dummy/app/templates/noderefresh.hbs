<h1>Async Tree</h1>

<p>
  It is possible to request child refresh for a tree node
</p>

<h2>Demo</h2>
<EmTree @model={{this.model}} @async={{true}} @selected={{this.selected}} @children={{this.getChildren}} />

{{#if this.selected}}
  <button {{on 'click'  this.addAndRefresh}} type="button">Add child to selected node and refresh</button>
{{/if}}

<h2>Markup</h2>

<div class="well line-example">
  <pre><code class="handlebars">
    &lt;EmTree @model={{this.model}} @async=&quot;true&quot; icons-per-type={{this.iconSet}}&gt;&lt;/EmTree&gt;
  </code></pre>
</div>

<h2>Code</h2>
<div class="well line-example">

</div>